version: 2

jobs:
  build:
    docker:
      - image: cimg/node:14.18.1
    working_directory: ~/my-app/my-app

    steps:
      - checkout:
          path: ~/my-app
      - run:
          name: Update NPM
          command: ls 
      - restore_cache:
          
          keys: 
            - dependency-cache-{{ checksum "my-app/package.json" }}

          
      # - run:
      #     name: Install Dependencies
      #     command: cd my-app && npm install
      # - save_cache:
      #     key: dependency-cache-{{ checksum "my-app/package.json" }}
      #     paths:
      #       - ./node_modules
workflows:
  version: 2
  build-deploy:
    jobs:
      - build