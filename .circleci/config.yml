version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:14.18.1
    working_directory: ~/my-app

    steps:
      - checkout
      - run:
          name: Update NPM
          command: ls my-app/
      - restore_cache:
          
          key: dependency-cache-{{ checksum "my-app/package.json" }}
          
      # - run:
      #     name: Install Dependencies
      #     command: cd my-app && npm install
      - save_cache:
          key: dependency-cache-{{ checksum "my-app/package.json" }}
          paths:
            - ./node_modules/cache
workflows:
  version: 2
  build-deploy:
    jobs:
      - build